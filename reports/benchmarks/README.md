# Local benchmark output

Store generated bot benchmark JSON and console logs here. Existing root
reports have been moved here unchanged; they describe earlier code and
should not be treated as measurements of the current physics.

Generate a fresh report with:

```sh
.venv/bin/python scripts/bench_bots.py --out reports/benchmarks/bench_results.json
```
