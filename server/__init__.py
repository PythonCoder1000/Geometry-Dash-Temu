"""Trigonometry Sprint server package.

The game's client talks to `server.app` over HTTP. See
``scripts/run_server.sh`` for local dev, or
``TRIGSPRINT_SERVER_URL=http://...`` to point the client at a remote.
"""
