"""Trigonometry Sprint game modules.

All client-side game code lives inside this package (editor, play, menus,
physics, ...). Entry points (``main.py``, ``test_game.py``) sit at the
repo root and import from here as ``from src.menus import run_menu`` etc.

Intra-package modules use relative imports (``from .constants import
CELL``) so the package is usable as ``src.*`` without path manipulation.
"""
